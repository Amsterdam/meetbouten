name: CI Checks

on:
  pull_request:
    branches: main

jobs:
  linting-and-tests:
    name: Templates
    uses: amsterdam/opdrachten-team-github-workflows/workflows/CI@v1
    with:
      repo_name: amsterdam
      project_name: meetbouten